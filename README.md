# CI / CD pipeline for a liferay module

## Configuration
### Jenkins
* The credentials for the NEXUS repository need to be configured as Jenkins global credentials. Create the following credentials of type text:
    * 'NEXUS_USERNAME'
    * 'NEXUS_PASSWORD'